(()=>{
const DB='https://avlozhwwvjqiypifoxox.supabase.co',KEY='sb_publishable_3FgdTAmKB8kw2QTrrVPA5g_vb1lya1d',BUCKET='bouquets';
const db=window.supabase?.createClient(DB,KEY,{auth:{persistSession:true,autoRefreshToken:true,detectSessionInUrl:false}});
const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const cats=async()=>{if(!db)throw Error('Supabase не загружен');const r=await db.from('categories').select('id,name,slug,description,image_url,sort_order,is_active').eq('is_active',true).order('sort_order').order('id');if(r.error)throw r.error;return r.data||[]};
const product=async id=>{const r=await db.from('products').select('id,category_id').eq('id',id).maybeSingle();if(r.error)throw r.error;return r.data};
const slugify=s=>String(s).toLowerCase().trim().replace(/[^a-z0-9а-яё]+/gi,'-').replace(/^-+|-+$/g,'')||'collection';
function selectField(){const f=document.getElementById('productForm');if(!f)return null;const all=[...f.querySelectorAll('select[data-sm-collection],#pcollection,[name="category_id"]')];let s=all[0]||null;all.slice(1).forEach(x=>x.closest('label')?.remove()||x.remove());if(!s){s=document.createElement('select');s.id='pcollection';s.name='category_id';s.dataset.smCollection='1';const l=document.createElement('label');l.id='smProductCollectionField';l.dataset.smCollectionField='1';l.textContent='Коллекция';l.appendChild(s);const file=document.getElementById('pfile');file?.closest('label')?.insertAdjacentElement('beforebegin',l)||f.appendChild(l)}else{s.id='pcollection';s.name='category_id';s.dataset.smCollection='1'}f.querySelectorAll('[data-sm-collection-field],#smProductCollectionField').forEach(l=>{if(!l.contains(s))l.remove()});return s}
async function fillSelect(selected){const s=selectField();if(!s)return;const data=await cats();s.innerHTML='<option value="">Без коллекции</option>'+data.map(c=>`<option value="${esc(c.id)}">${esc(c.name)}</option>`).join('');s.value=selected?String(selected):''}
async function saveCat(id,value){if(!db)throw Error('Supabase не загружен');const category_id=value?Number(value):null;const r=await db.from('products').update({category_id}).eq('id',id).select('id,category_id').maybeSingle();if(r.error)throw r.error;if(!r.data)throw Error('Букет не найден при сохранении коллекции');if(String(r.data.category_id??'')!==String(category_id??''))throw Error('Коллекция не сохранилась');return r.data}
function installSave(){const f=document.getElementById('productForm');if(!f||f.__smSaveInstalled||typeof f.onsubmit!=='function')return false;const original=f.onsubmit;f.__smSaveInstalled=true;f.onsubmit=async e=>{const s=selectField(),chosen=s?.value||'',pid=document.getElementById('pid'),name=document.getElementById('pname'),existing=pid?.value||'';await original.call(f,e);let id=existing;if(!id&&name?.value.trim()){const rows=await db.from('products').select('id').eq('slug',slugify(name.value)).order('id',{ascending:false}).limit(1);if(rows.error)throw rows.error;id=rows.data?.[0]?.id||''}if(!id)throw Error('Не найден ID букета для сохранения коллекции');await saveCat(id,chosen);const check=await product(id);if(String(check?.category_id??'')!==String(chosen||''))throw Error('Коллекция не сохранилась');const msg=document.getElementById('pmsg');if(msg)msg.textContent='Букет и коллекция сохранены';if(pid&&existing){await fillSelect(chosen)}};return true}
function bindEdit(){const list=document.getElementById('productsList');if(!list||list.__smEdit)return;list.__smEdit=true;list.addEventListener('click',e=>{const b=e.target.closest('[data-edit]');if(!b)return;setTimeout(async()=>{const s=selectField();if(!s)return;try{const p=await product(b.dataset.edit);await fillSelect(p?.category_id||'')}catch(err){console.error(err)}} ,50)})}
function bindDelete(){const list=document.getElementById('productsList');if(!list||list.__smDelete)return;list.__smDelete=true;const add=()=>list.querySelectorAll('[data-edit]').forEach(e=>{const row=e.closest('.row');if(!row||row.querySelector('[data-sm-delete]'))return;const b=document.createElement('button');b.type='button';b.className='danger';b.dataset.smDelete=e.dataset.edit;b.textContent='Удалить букет';b.style.marginLeft='8px';e.insertAdjacentElement('afterend',b)});list.addEventListener('click',async e=>{const b=e.target.closest('[data-sm-delete]');if(!b)return;e.preventDefault();e.stopPropagation();if(!confirm('Удалить этот букет? Существующие заказы сохранятся.'))return;const row=b.closest('.row');b.disabled=true;b.textContent='Удаление…';try{const r=await db.from('products').delete().eq('id',b.dataset.smDelete);if(r.error)throw r.error;row?.remove()}catch(err){b.disabled=false;b.textContent='Удалить букет';alert(err.message)}});add();new MutationObserver(add).observe(list,{childList:true,subtree:true})}
async function collectionsEditor(){const box=document.getElementById('siteEditor');if(!box||box.classList.contains('hidden')||document.getElementById('smCollectionsManager'))return;const title=box.querySelector('h3')?.textContent||'',active=document.querySelector('.site-tile.active')?.dataset.block||'';if(active!=='Коллекции'&&!/коллекц/i.test(title))return;const m=document.createElement('div');m.id='smCollectionsManager';m.className='field';m.innerHTML='<div class="actions"><h4>Управление коллекциями</h4><button type="button" id="smNewCollection">Добавить коллекцию</button></div><div id="smCollectionsList"></div><div id="smCollectionForm" class="hidden"><input id="smCid" type="hidden"><label>Название<input id="smCname"></label><label>Описание<textarea id="smCdesc" rows="3"></textarea></label><label>Фото коллекции<input id="smCfile" type="file" accept="image/*"></label><button type="button" id="smSaveCollection">Сохранить коллекцию</button><button type="button" id="smCancelCollection" class="secondary">Отмена</button><p id="smCmsg"></p></div>';box.appendChild(m);const list=document.getElementById('smCollectionsList'),form=document.getElementById('smCollectionForm');const draw=async()=>{const data=await cats();list.innerHTML=data.map(c=>`<div class="row"><b>${esc(c.name)}</b> <button type="button" data-cat-edit="${c.id}">Изменить</button> <button type="button" class="danger" data-cat-delete="${c.id}">Удалить</button></div>`).join('')||'<p>Коллекций пока нет.</p>';await fillSelect(document.getElementById('pcollection')?.value||'');list.querySelectorAll('[data-cat-edit]').forEach(b=>b.onclick=()=>{const c=data.find(x=>String(x.id)===b.dataset.catEdit);smCid.value=c.id;smCname.value=c.name;smCdesc.value=c.description||'';smCfile.value='';form.classList.remove('hidden')});list.querySelectorAll('[data-cat-delete]').forEach(b=>b.onclick=async()=>{if(!confirm('Удалить коллекцию? Букеты останутся без коллекции.'))return;const u=await db.from('products').update({category_id:null}).eq('category_id',b.dataset.catDelete);if(u.error)throw u.error;const d=await db.from('categories').delete().eq('id',b.dataset.catDelete);if(d.error)throw d.error;await draw()})};smNewCollection.onclick=()=>{smCid.value='';smCname.value='';smCdesc.value='';smCfile.value='';form.classList.remove('hidden')};smCancelCollection.onclick=()=>form.classList.add('hidden');smSaveCollection.onclick=async()=>{try{const id=smCid.value,name=smCname.value.trim();if(!name)throw Error('Введите название коллекции');const payload={name,slug:slugify(name),description:smCdesc.value.trim()};if(id){const r=await db.from('categories').update(payload).eq('id',id);if(r.error)throw r.error}else{payload.is_active=true;const q=await db.from('categories').select('sort_order').order('sort_order',{ascending:false}).limit(1).maybeSingle();if(q.error)throw q.error;payload.sort_order=Number(q.data?.sort_order||0)+1;const r=await db.from('categories').insert(payload);if(r.error)throw r.error}form.classList.add('hidden');await draw()}catch(e){document.getElementById('smCmsg').textContent=e.message}};await draw()}
function start(){const tick=()=>{selectField();bindEdit();bindDelete();installSave();collectionsEditor()};tick();new MutationObserver(tick).observe(document.body,{childList:true,subtree:true})}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',start);else start();
})();