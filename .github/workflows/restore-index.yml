name: Restore site index

on:
  push:
    branches: [main]

permissions:
  contents: write

jobs:
  restore:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Restore last known good index
        run: |
          git checkout 273dafa45dbabd7e8d89574760c6ce7290f001e9 -- index.html
          if git diff --cached --quiet -- index.html; then
            echo "index.html is already restored"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git commit -m "Restore working site index"
          git push
